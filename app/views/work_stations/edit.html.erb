<%= form_with model: @work_station do |form| %>
  <%= form.text_field :name %>
  <%= form.collection_select :status,
                             WorkStation.statuses.keys.map { |key| [key.humanize, key] }, :last, :first, prompt: 'Choose a status' %>
  <%= form.submit %>
<% end %>
