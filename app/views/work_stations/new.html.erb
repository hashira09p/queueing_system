<div class="container d-flex justify-content-center align-items-center vh-70">
  <div class="card" style="width: 25rem;">
    <div class="card-body">
      <h1 class="card-title text-center mb-4">New WorkStation</h1>
      <%= form_with model: @work_station, class: 'needs-validation', novalidate: true do |form| %>

        <div class="mb-3">
          <%= form.label :name, class: 'form-label' %>
          <%= form.text_field :name, class: 'form-control', placeholder: 'Enter Work Station Name', required:true %>
        </div>

        <div class="mb-3">
          <%= form.label :status, class: 'form-label' %>
          <%= form.collection_select :status,
                                     WorkStation.statuses.keys.map { |key| [key.humanize, key] },
                                     :last, :first,
                                     { prompt: 'Choose a status' },
                                     { class: 'form-select', required: true} %>
        </div>

        <%= form.submit 'Submit', class: 'btn btn-primary w-100' %>
      <% end %>
    </div>
  </div>
</div>
